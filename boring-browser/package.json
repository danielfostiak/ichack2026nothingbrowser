{
  "name": "boring-browser",
  "version": "1.0.0",
  "description": "A browser that transforms websites into minimal UIs with zero flicker",
  "main": "dist/main.js",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && electron dist/main.js",
    "start": "electron dist/main.js"
  },
  "keywords": ["electron", "browser", "minimal"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.0",
    "electron": "^28.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "jsdom": "^23.0.0"
  }
}
